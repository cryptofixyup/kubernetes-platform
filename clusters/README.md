# Clusters

Place environment-specific or cluster-specific configuration in this directory.

Typical contents may include:

- development, staging, and production overlays
- cluster bootstrap material
- per-cluster policy and namespace definitions

Keep shared platform components in `/platform`.
